# AI 招聘系统 v2.0 — 代码质量评估

> 2026-07-08 | 综合: 7.2/10 | 目标: 9.0

---

## 评分

| 维度 | 得分 | 说明 |
|------|:---:|------|
| 架构与模块边界 | 8 | deps + ApplicationService 已落地，路由纯胶水 |
| 代码坏味道 | 6 | P0 bug + 冗余解析 + ORM 不一致 |
| 测试与可维护性 | 7 | 15 个服务层测试，缺路由/AI 集成测试 |
| 演进（屎山） | 7 | 服务层收口，未来改动不会往路由塞逻辑 |
| **综合** | **7.2** | |

---

## 改进成果（已落地）

```
✅ deps.py                    权限集中 require_roles / require_hr_or_above
✅ 4 个路由文件                内联角色检查全部消除，改用 Depends
✅ ApplicationService         218 行领域服务，投递/状态/打分/重试
✅ applications.py            路由瘦身，异常映射到 HTTP
✅ conftest.py                内存 SQLite 测试夹具
✅ test_application_service  15 个测试：提交/流转/异常/打分写入
```

---

## 卡分清单（6 项 → 9.0）

### P0 · 1 项 · 7.2→7.7

| # | 文件 | 问题 |
|---|------|------|
| 1 | `services/ai_service.py:27-38` | `ScoreResult` 缺 `recommendation`，打分成功必崩 |

### P1 · 3 项 · 7.7→8.3

| # | 文件 | 问题 |
|---|------|------|
| 2 | `services/ai_service.py:117-148` | `parse_resume_text` / `_extract_pdf` / `_extract_docx` 与 `utils/file_parser.py` 功能重叠 |
| 3 | `services/ai_service.py:14-16` | `import Path / pdfplumber / docx` 删冗余后可移除 |
| 4 | `models/llm_config.py` | 全文件 `Column()` 风格，其余模型用 `Mapped[]` |

### P2 · 3 项 · 8.3→8.8

| # | 文件 | 问题 |
|---|------|------|
| 5 | `routes/jobs.py:28` | 魔法数字 `100`，应提进 `settings.MIN_JD_LENGTH` |
| 6 | `routes/resumes.py:63` | 魔法数字 `500`，应提进 `settings.RESUME_PREVIEW_LENGTH` |
| 7 | `routes/jobs.py:9` | `require_any_staff` 已导入但 0 处使用 |

### 测试 · 3 项 · 8.8→9.0

| # | 文件 | 内容 |
|---|------|------|
| 8 | `tests/test_routes.py` | 路由集成测试（注册/登录/CRUD/状态流转端到端） |
| 9 | `tests/test_ai_service.py` | AI mock 测试（钳位/推荐等级/JSON提取/正常+降级） |
| 10 | `harness/spec/*/` | import 路径从 `services.*` 改为 `app.*` |

---

## 致命调用链

```
applications.py:155  →  ai_result.recommendation
                              ↑
                   ai_service.py:ScoreResult — 无此字段
                              ↑
                   打分成功 → AttributeError 500
                   打分失败 → DEFAULT_SCORE 也无此字段 → 同样 500
```

**两条链路，一个结果。改第一项即修复。**
