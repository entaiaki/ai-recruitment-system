# AI 招聘系统 — 剩余优化清单

> 当前评分：7.0/10 | 评估日期：2026-07-08

---

## 🐛 P0 — Bug 修复（运行时必崩）

### `ScoreResult` 缺少 `recommendation` 字段

**位置**：`services/ai_service.py:35` + `routes/applications.py:181`

**问题**：路由里调用了 `ai_result.recommendation`，但 `ScoreResult` dataclass 没有这个字段——运行到打分必定 `AttributeError`。

**修复**：

```python
# services/ai_service.py — ScoreResult 补字段

@dataclass
class ScoreResult:
    skills:       float = 0
    experience:   float = 0
    education:    float = 0
    potential:    float = 0
    stability:    float = 0
    total:        float = 0
    summary:      str = ""
    strengths:    list = field(default_factory=list)
    weaknesses:   list = field(default_factory=list)
    recommendation: str = ""              # ← 补上这行


DEFAULT_SCORE = ScoreResult(
    skills=0, experience=0, education=0, potential=0, stability=0, total=0,
    summary="AI 打分失败，请人工审核",
    strengths=[],
    weaknesses=["AI 自动打分未能完成，需人工评估"],
    recommendation="Not Recommended",     # ← 补上这行
)
```

然后在 `score_resume` 方法里，解析 JSON 后根据 `total` 计算推荐等级：

```python
# services/ai_service.py — score_resume 方法末尾，return 之前

def _calc_recommendation(total: float) -> str:
    if total >= 85:  return "Highly Recommended"
    if total >= 70:  return "Recommended"
    if total >= 50:  return "Maybe"
    return "Not Recommended"

# 在构建 ScoreResult 时加上
return ScoreResult(
    skills=data["skills"],
    experience=data["experience"],
    education=data["education"],
    potential=data["potential"],
    stability=data["stability"],
    total=data["total"],
    summary=data.get("summary", ""),
    strengths=data.get("strengths", []),
    weaknesses=data.get("weaknesses", []),
    recommendation=_calc_recommendation(data["total"]),   # ← 补上
)
```

---

## 🟠 P1 — 建议修复

### 1. `llm_config.py` 旧式 ORM 风格不统一

**位置**：`models/llm_config.py`

**问题**：整个项目都用 SQLAlchemy 2.0 `Mapped[]` 风格，就它用旧式 `Column()`。

```python
# ❌ 当前：旧式写法
from sqlalchemy import Column, Integer, String, Boolean, DateTime, func
from app.core.database import Base

class LLMConfig(Base):
    __tablename__ = "llm_configs"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(100), nullable=False)
    base_url = Column(String(500), nullable=False)
    api_key = Column(String(500), nullable=False)
    model_name = Column(String(200), nullable=False)
    timeout = Column(Integer, default=120)
    max_retries = Column(Integer, default=3)
    is_active = Column(Boolean, default=False)
    created_at = Column(DateTime, server_default=func.now())
    updated_at = Column(DateTime, server_default=func.now(), onupdate=func.now())
```

```python
# ✅ 修复：统一 Mapped 风格
from datetime import datetime, timezone
from sqlalchemy import String, Integer, Boolean, DateTime
from sqlalchemy.orm import Mapped, mapped_column
from app.core.database import Base

class LLMConfig(Base):
    __tablename__ = "llm_configs"

    id: Mapped[int] = mapped_column(primary_key=True, index=True)
    name: Mapped[str] = mapped_column(String(100))
    base_url: Mapped[str] = mapped_column(String(500))
    api_key: Mapped[str] = mapped_column(String(500))
    model_name: Mapped[str] = mapped_column(String(200))
    timeout: Mapped[int] = mapped_column(Integer, default=120)
    max_retries: Mapped[int] = mapped_column(Integer, default=3)
    is_active: Mapped[bool] = mapped_column(Boolean, default=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), default=lambda: datetime.now(timezone.utc))
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), default=lambda: datetime.now(timezone.utc),
        onupdate=lambda: datetime.now(timezone.utc))
```

---

### 2. `ai_service.py` 里重复实现了文件解析

**位置**：`services/ai_service.py` 的 `parse_resume_text`、`_extract_pdf`、`_extract_docx`（约 40 行）

**问题**：`utils/file_parser.py` 已经做了文件解析。`ai_service` 又 import `pdfplumber` 和 `docx` 自己拆了一份。

```python
# ❌ ai_service.py 里有这些
import pdfplumber
from docx import Document

def parse_resume_text(self, file_path: str) -> str:
    # ... 40 行解析逻辑

def _extract_pdf(self, path: Path) -> str:
    # ...

def _extract_docx(self, path: Path) -> str:
    # ...
```

```python
# ✅ 修复：删掉重复的解析方法，改为调用 utils
from app.utils.file_parser import parse_resume

# 所有原本调 ai_service.parse_resume_text(path) 的地方改为：
text = parse_resume(path)
```

然后从 `ai_service.py` 删掉：
- `import pdfplumber`（如果不再其他用途）
- `from docx import Document`
- `from pathlib import Path`（如果不再其他用途）
- `parse_resume_text`、`_extract_pdf`、`_extract_docx` 三个方法

---

### 3. `routes/jobs.py` 无用 import

**位置**：`routes/jobs.py:9`

```python
# ❌ 导入了但从未使用
from app.api.deps import require_hr_or_above, require_any_staff
#                                         ^^^^^^^^^^^^^^^^ 删掉
```

---

## 🟡 P2 — 可暂缓

### 4. 魔法数字 `100` / `500`

| 位置 | 硬编码 | 改为 |
|------|--------|------|
| `routes/jobs.py:31` | `len(data.jd_body.strip()) < 100` | `settings.MIN_JD_LENGTH` |
| `routes/resumes.py:56` | `(parsed_text or "")[:500]` | `settings.RESUME_PREVIEW_LENGTH` |

```python
# core/config.py — 补字段
class Settings(BaseSettings):
    # ... 已有字段 ...
    MIN_JD_LENGTH: int = 100
    RESUME_PREVIEW_LENGTH: int = 500

# routes/jobs.py:31
if len(data.jd_body.strip()) < settings.MIN_JD_LENGTH:
    raise HTTPException(422, f"JD 正文不少于 {settings.MIN_JD_LENGTH} 字")

# routes/resumes.py:56
result.parsed_text_preview = (parsed_text or "")[:settings.RESUME_PREVIEW_LENGTH]
```

---

### 5. Harness 测试 import 路径错误

**位置**：`harness/spec/scoring/` 和 `harness/spec/pipeline/` 和 `harness/spec/resume/`

```python
# ❌ 当前：引用不存在的模块路径
from services.llm_scorer import score_resume
from services.pipeline import CandidatePipeline
from services.resume_parser import parse_resume
```

```python
# ✅ 修复为实际路径
from app.services.ai_service import ai_service, ScoreResult
from app.services.application_service import ApplicationService
from app.utils.file_parser import parse_resume
```

---

### 6. Harness `conftest.py` 缺少 fixtures 目录

`harness/spec/resume/test_parser.py` 和 `harness/spec/scoring/test_score_stability.py` 引用了不存在的 `FIXTURES_DIR`。需要在 `harness/` 下建一个 `fixtures/` 目录放测试用的 PDF/DOCX 样本。

---

### 7. `ai_service.py` 偏长（270行）

可进一步拆分，但这不是 bug，只是可读性优化。如果将来要改，可以把 Prompt 模板和分数解析逻辑各拆一个文件：

```
services/
├── ai_service.py          ← 只保留 score_resume + _get_client（~80行）
├── scoring_prompt.py      ← SCORING_PROMPT 模板
└── scoring_parser.py      ← _extract_json + _clamp + _calc_recommendation
```

---

### 8. 补测试：AI 打入 Mock 测试

当前 15 个测试覆盖了 `ApplicationService`，但没有覆盖 `AIService`。可以补一个 `test_ai_service.py`，mock 掉 `_get_client` 返回的 OpenAI client，验证 JSON 解析和分数钳位逻辑。

```python
# tests/test_ai_service.py（骨架）
import pytest
from unittest.mock import AsyncMock, patch

async def test_score_resume_success():
    mock_response = AsyncMock()
    mock_response.choices = [AsyncMock()]
    mock_response.choices[0].message.content = '{"skills":20,"experience":22,...}'

    with patch.object(ai_service, '_get_client',
                      return_value=(mock_client, "gpt-4", 30, 3)):
        result = await ai_service.score_resume(
            db=mock_db, resume_text="...", job_title="...",
            job_requirements="...")
        assert result.total > 0
```

---

## 📊 汇总

| # | 优先级 | 问题 | 预估 |
|---|:---:|------|:---:|
| 1 | 🔴 P0 | `ScoreResult` 缺 `recommendation` — **运行时必崩** | 5 分钟 |
| 2 | 🟠 P1 | `llm_config.py` 旧式 ORM 风格 | 5 分钟 |
| 3 | 🟠 P1 | `ai_service.py` 重复文件解析逻辑 | 10 分钟 |
| 4 | 🟠 P1 | `jobs.py` 无用 import | 10 秒 |
| 5 | 🟡 P2 | 魔法数字 `100` / `500` | 5 分钟 |
| 6 | 🟡 P2 | Harness import 路径错误 | 10 分钟 |
| 7 | 🟡 P2 | `ai_service.py` 偏长 | 可选 |
| 8 | 🟡 P2 | 补 AI mock 测试 | 可选 |

**P0+P1 合计：约 20 分钟，修完优雅度可到 7.5。**
